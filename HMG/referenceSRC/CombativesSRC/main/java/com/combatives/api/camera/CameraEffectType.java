package com.combatives.api.camera;

/** Tuned Combatives camera behaviors, not raw camera transforms. */
public enum CameraEffectType {
    EXPLOSION, LANDING, IMPACT, WEAPON_FIRE, VEHICLE_COLLISION, ENVIRONMENTAL_RUMBLE, SUPPRESSION, CUSTOM
}
